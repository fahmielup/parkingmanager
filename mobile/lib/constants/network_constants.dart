/// Constants used to reach the local "Parking Manager" PC web backend.
/// 10.0.2.2 is the special Android Emulator IP that routes to the host PC.
class NetworkConstants {
  const NetworkConstants._();

  static const String baseUrl = 'http://10.0.2.2:5000';
}
